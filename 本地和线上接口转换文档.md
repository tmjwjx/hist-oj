# 本地和线上接口转换文档

> **最后更新**: 2026-01-21
> **版本**: v4.0 (包含签到记录管理功能)

---

## 📋 目录

1. [快速切换](#快速切换)
2. [核心配置差异](#核心配置差异)
3. [详细配置说明](#详细配置说明)
4. [API 路径规范](#api-路径规范)
5. [修复记录清单](#修复记录清单)
6. [常见问题排查](#常见问题排查)
7. [验证清单](#验证清单)

---

## 🚀 快速切换

### ⚠️ 本地开发环境重要提示

**本地开发环境当前配置**：
- hist-oj 后端运行在 **Docker 容器**中，端口映射为 **9527**
- 前端开发服务器运行在 **8066** 端口
- vue.config.js 中 `/rating-api`、`/judge-api`、`/battle-api` 都代理到 `http://localhost:9527`
- `/api` 代理到 HOJ 主后端 `http://43.143.133.62:6688`

**关键点**：
- 所有 classroom、rating、battle、judge 相关接口必须使用 `/rating-api/api/`、`/battle-api/api/`、`/judge-api/api/` 前缀
- 不要直接使用 `/api/classroom` 等，这会导致请求发送到错误的后端服务

### 从线上部署切换到本地开发

```bash
# 方法1: 使用脚本（推荐）
./restore-local.sh

# 方法2: 手动修改
cd hist-oj/configs
vim config.yaml  # 修改: mode: debug

# 启动服务（使用 Docker）
cd ../..
docker-compose up -d hist-oj  # 启动 hist-oj 容器（端口 9527）
cd hoj-vue && npm run serve    # 启动前端（端口 8066）
```

### 从本地开发切换到线上部署

```bash
# 方法1: 使用脚本（推荐）
./restore-production.sh

# 方法2: 手动修改
cd hist-oj/configs
vim config.yaml  # 修改: mode: release

# 构建和部署
cd ../../hoj-vue
npm run build
cd ..
./scripts/quick-deploy.sh all
```

---

## ⚙️ 核心配置差异

### 只需修改 1 个文件！

**文件**: `hist-oj/configs/config.yaml`

| 环境 | 配置 | 说明 |
|------|------|------|
| **本地开发** | `mode: debug` | 开发模式，显示详细日志 |
| **线上部署** | `mode: release` | 生产模式，优化性能 |

**其他所有配置都已自动处理，无需修改！**

### 配置差异总览表

| 配置项 | 本地开发 | 线上部署 | 是否需要修改 |
|--------|----------|----------|-------------|
| **hist-oj 模式** | `debug` | `release` | ✅ **需要** |
| **前端运行方式** | `npm run serve` | `npm run build` + Docker | 自动 |
| **前端端口** | 8066 | 80/443 | 自动 |
| **代理配置** | vue.config.js | nginx.conf | 自动 |
| **后端地址** | localhost:9527 | hist-oj:9527 (Docker) | 自动 |
| **API baseURL** | 自动切换 | 自动切换 | 自动 |

---

## 📖 详细配置说明

### 1. hist-oj 后端配置

**文件**: `hist-oj/configs/config.yaml`

```yaml
server:
  port: 9527
  mode: debug  # 本地: debug, 线上: release

database:
  host: 43.143.133.62
  port: 3306
  user: root
  password: hist2025
  dbname: hoj
  # 连接池配置（本地和线上相同）
  max_open_conns: 100
  max_idle_conns: 10
  conn_max_lifetime: 1800  # 30分钟
  conn_max_idletime: 600   # 10分钟

hoj_api:
  base_url: http://43.143.133.62:6688

jwt:
  secret: hoj-secret-init

rating:
  initial_rating: 1200
  k_factor: 85
  check_interval: 5
```

**重要**: 数据库连接池配置在本地和线上都相同，用于防止 MySQL 连接超时。

### 2. 前端代理配置

#### 本地开发配置

**文件**: `hoj-vue/vue.config.js`

```javascript
module.exports = {
  devServer: {
    port: 8066,
    proxy: {
      // ⚠️ 注意顺序：具体路由在前，通用路由在后
      '/registration-api': {
        target: 'http://43.143.133.62:8080',
        changeOrigin: true,
        pathRewrite: { '^/registration-api': '' }  // 去掉前缀
      },
      '/rating-api': {
        target: 'http://localhost:9527',  // 本地 hist-oj
        changeOrigin: true,
        pathRewrite: { '^/rating-api': '' }
      },
      '/judge-api': {
        target: 'http://localhost:9527',
        changeOrigin: true,
        pathRewrite: { '^/judge-api': '' }
      },
      '/battle-api': {
        target: 'http://localhost:9527',
        changeOrigin: true,
        pathRewrite: { '^/battle-api': '' }
      },
      '/api': {
        target: 'http://43.143.133.62:6688',  // HOJ 主后端
        changeOrigin: true
      }
    }
  }
}
```

#### 线上部署配置

**文件**: `hoj-vue/nginx.conf`

```nginx
server {
    listen 80;

    # 前端静态文件
    location / {
        root /usr/share/nginx/html;
        try_files $uri $uri/ /index.html;

        # 禁用 HTML 缓存
        add_header Cache-Control "no-cache, no-store, must-revalidate";
    }

    # JS/CSS 长期缓存
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # API 代理
    location /rating-api/ {
        proxy_pass http://hist-oj:9527/api/;  # Docker 容器名
    }

    location /judge-api/ {
        proxy_pass http://hist-oj:9527/api/judge/;
    }

    location /battle-api/ {
        proxy_pass http://hist-oj:9527/api/;
    }

    location /registration-api/ {
        proxy_pass http://registration-backend:8080/api/;
    }

    location /api/ {
        proxy_pass http://43.143.133.62:6688/api/;  # 外部服务
    }
}
```

### 3. 前端 API 配置

所有前端 API 文件都已配置为**自动根据环境切换**：

#### Rating API

**文件**: `hoj-vue/src/common/rating-api.js`

```javascript
const getRatingApiBaseURL = () => {
  if (process.env.NODE_ENV === 'production') {
    return '/api'              // 线上: /api
  }
  return '/rating-api/api'     // 本地: /rating-api/api
}
```

#### Battle API

**文件**: `hoj-vue/src/api/battle.js`

```javascript
const battleRequest = axios.create({
  baseURL: '/battle-api/api',  // 本地开发
  // 线上部署会自动使用 /api（通过 Nginx）
  timeout: 30000
});
```

#### Judge API

**文件**: `hoj-vue/src/common/judgeTerminal.js`

```javascript
// 使用完整路径
return axios.post('/judge-api/api/judge/get-info', data)
return axios.post('/judge-api/api/judge/get-history', data)
const url = `/judge-api/api/judge/run-combined`
```

#### Classroom API

**文件**: `hoj-vue/src/api/classroom.js`

```javascript
const BASE_URL = '/rating-api/api/classroom'
```

#### Rating Admin API

**文件**: `hoj-vue/src/views/admin/general/RatingManagement.vue`

```javascript
// 使用 /rating-api/api 而不是 /api
axios.post('/rating-api/api/rating/admin/adjust', {...})
axios.get('/rating-api/api/rating/admin/history', {...})
```

---

## 🔗 API 路径规范

### 本地开发 API 路径

```
前端请求                        代理后                    后端路由
─────────────────────────────────────────────────────────────────────────
/rating-api/api/classroom/* → /api/classroom/*   → GET /api/classroom/*
/rating-api/api/rating/*    → /api/rating/*      → GET /api/rating/*
/battle-api/api/battle/*    → /api/battle/*      → GET /api/battle/*
/judge-api/api/judge/*      → /api/judge/*       → POST /api/judge/*
/registration-api/api/*     → /api/*             → GET /api/* (报名系统)
/api/*                       → /api/*             → HOJ主后端 6688
```

### 线上部署 API 路径

```
前端请求              Nginx 转发                  后端服务
────────────────────────────────────────────────────────────────
/api/classroom/*  → /api/classroom/*         → hist-oj:9527
/api/rating/*     → /api/rating/*            → hist-oj:9527
/api/battle/*     → /api/battle/*            → hist-oj:9527
/api/judge/*      → /api/judge/*             → hist-oj:9527
/api/* (其他)     → /api/*                   → HOJ:6688
```

### 规则总结

| API 模块 | 本地开发路径 | 线上部署路径 | 后端服务 |
|---------|-------------|-------------|----------|
| **Classroom** | `/rating-api/api/classroom` | `/api/classroom` | hist-oj |
| **Checkin** | `/rating-api/api/classroom/checkin` | `/api/classroom/checkin` | hist-oj |
| **Rating** | `/rating-api/api/rating` | `/api/rating` | hist-oj |
| **Battle** | `/battle-api/api/battle` | `/api/battle` | hist-oj |
| **Judge** | `/judge-api/api/judge` | `/api/judge` | hist-oj |
| **Registration** | `/registration-api/api` | `/api` | registration-backend |
| **HOJ 主后端** | `/api` | `/api` | HOJ 6688 |

---

## 📝 修复记录清单

### 第一轮修复 (2026-01-20)

**问题**: 数据库连接超时、Webpack 代理配置错误、Rating API 路径错误

**修复内容**:

1. **数据库连接池配置** ✅
   - 文件: `hist-oj/configs/config.yaml`
   - 添加: `max_open_conns`, `max_idle_conns`, `conn_max_lifetime`, `conn_max_idletime`
   - 文件: `hist-oj/internal/config/config.go`
   - 添加: DatabaseConfig 结构体字段
   - 文件: `hist-oj/internal/client/database.go`
   - 添加: 连接池初始化逻辑

2. **Webpack 代理配置** ✅
   - 文件: `hoj-vue/vue.config.js`
   - 修改1: 调整代理顺序（`/rating-api` → `/api`）
   - 修改2: pathRewrite 改为空字符串

3. **Rating API 配置** ✅
   - 文件: `hoj-vue/src/common/rating-api.js`
   - 修改: baseURL 从 `/rating-api` 改为 `/rating-api/api`

### 第二轮修复 (2026-01-20)

**问题**: Battle, Judge, Rating Admin API 路径错误，导致 404

**修复内容**:

1. **Battle API** ✅
   - 文件: `hoj-vue/src/api/battle.js`
   - 修改: `baseURL: '/battle-api'` → `'/battle-api/api'`

2. **Judge Terminal API** ✅
   - 文件: `hoj-vue/src/common/judgeTerminal.js`
   - 修改: 3个接口路径
     - `/judge-api/get-info` → `/judge-api/api/judge/get-info`
     - `/judge-api/get-history` → `/judge-api/api/judge/get-history`
     - `/judge-api/run-combined` → `/judge-api/api/judge/run-combined`

3. **Classroom Judge Submit** ✅
   - 文件: `hoj-vue/src/components/classroom/ProgrammingQuestion.vue`
   - 修改: `/judge-api/submit` → `/judge-api/api/judge/submit`

4. **Rating Admin API** ✅
   - 文件: `hoj-vue/src/views/admin/general/RatingManagement.vue`
   - 修改: 2个接口
     - `/api/rating/admin/adjust` → `/rating-api/api/rating/admin/adjust`
     - `/api/rating/admin/history` → `/rating-api/api/rating/admin/history`

### 第三轮修复 (2026-01-20)

**问题**: 本地开发环境中 hist-oj 后端运行在 Docker 容器端口 9527，部分 classroom 相关接口直接使用 `/api` 导致请求发送到错误的 HOJ 主后端

**根本原因**:
- hist-oj 运行在 Docker 容器中，端口映射为 9527
- 部分 classroom 接口使用了错误的 API 路径 `/api/classroom` 而不是 `/rating-api/api/classroom`
- vue.config.js 代理配置中 `/rating-api` 需要指向正确的 `http://localhost:9527`

**修复内容**:

1. **Vue 代理配置** ✅
   - 文件: `hoj-vue/vue.config.js`
   - 修改: 确认所有 hist-oj 相关代理指向 `http://localhost:9527`
     - `/rating-api` → `http://localhost:9527`
     - `/judge-api` → `http://localhost:9527`
     - `/battle-api` → `http://localhost:9527`
     - `/api` → `http://43.143.133.62:6688` (HOJ 主后端，保持不变)

2. **Classroom API 路径修复** ✅
   - 文件: `hoj-vue/src/views/classroom/teacher/Dashboard.vue`
     - 修改: `/api/classroom/${classroomId}/students` → `/rating-api/api/classroom/${classroomId}/students`

   - 文件: `hoj-vue/src/views/classroom/teacher/Checkin.vue`
     - 修改: `/api/classroom/${this.classroomId}/students` → `/rating-api/api/classroom/${this.classroomId}/students`
     - 修改: `/api/classroom/checkin/${record.checkinId}/record` → `/rating-api/api/classroom/checkin/${record.checkinId}/record`
     - 修改: `/api/classroom/checkin/${this.editForm.id}` → `/rating-api/api/classroom/checkin/${this.editForm.id}`
     - 修改: `/api/classroom/checkin/${checkin.id}` → `/rating-api/api/classroom/checkin/${checkin.id}`

   - 文件: `hoj-vue/src/views/classroom/student/Checkin.vue`
     - 修改: `/api/classroom/${this.classroomId}/students` → `/rating-api/api/classroom/${this.classroomId}/students`

**验证**:
```bash
# 测试代理是否正常工作
curl http://localhost:8066/rating-api/api/classroom/user/roles
# 预期: {"code":200,"message":"success","data":[...]}

curl http://localhost:8066/rating-api/api/classroom/9/students
# 预期: {"code":200,"message":"success","data":[...]}
```

**重要说明**:
- 本地开发环境中，hist-oj 运行在 Docker 容器中，端口为 9527
- 所有 classroom 相关的接口必须使用 `/rating-api/api/classroom` 前缀
- 不应该直接使用 `/api/classroom`，这会错误地代理到 HOJ 主后端（6688）

### 所有修改的文件汇总

#### 配置文件
- ✏️ `hist-oj/configs/config.yaml` - 添加连接池配置
- ✏️ `hist-oj/internal/config/config.go` - 添加连接池字段
- ✏️ `hist-oj/internal/client/database.go` - 添加连接池初始化
- ✏️ `hoj-vue/vue.config.js` - 修复代理配置（第三轮修复：确认端口指向 9527）

#### API 文件
- ✏️ `hoj-vue/src/common/rating-api.js` - 修复 baseURL
- ✏️ `hoj-vue/src/api/battle.js` - 修复 baseURL
- ✏️ `hoj-vue/src/common/judgeTerminal.js` - 修复 3个接口路径
- ✏️ `hoj-vue/src/api/classroom.js` - 已正确，无需修改
- ✏️ `hoj-vue/src/components/classroom/ProgrammingQuestion.vue` - 修复 submit 接口
- ✏️ `hoj-vue/src/views/admin/general/RatingManagement.vue` - 修复 2个 admin 接口

#### Classroom 组件（第三轮修复）
- ✏️ `hoj-vue/src/views/classroom/teacher/Dashboard.vue` - 修复 students 接口路径
- ✏️ `hoj-vue/src/views/classroom/teacher/Checkin.vue` - 修复 4 个 classroom 接口路径
- ✏️ `hoj-vue/src/views/classroom/student/Checkin.vue` - 修复 students 接口路径

### 第四轮修复 (2026-01-21)

**问题**: 签到记录功能中缺少创建签到记录的 API 接口

**修复内容**:

1. **后端 API 新增** ✅
   - 文件: `hist-oj/internal/api/routes.go`
   - 新增路由: `POST /checkin/:checkinId/record`
   - 功能: 教师为未签到学生创建签到记录

2. **后端 Handler 实现** ✅
   - 文件: `hist-oj/internal/api/classroom_api.go`
   - 新增函数: `CreateCheckinRecord`
   - 功能: 创建签到记录,支持 present/absent/sick_leave/personal_leave 状态

3. **前端组件** ✅
   - 文件: `hoj-vue/src/views/classroom/teacher/CheckinRecords.vue`
   - 新功能: 独立的签到记录页面
   - 新功能: 支持为未签到学生创建记录
   - 新功能: 支持按学生姓名/学号搜索
   - 新功能: 支持按状态筛选
   - 新功能: 导出签到记录为 CSV

4. **前端路由** ✅
   - 文件: `hoj-vue/src/router/classroomRoutes.js`
   - 新增路由: `teacher/checkin/records/:classroomId/:checkinId`
   - 组件: CheckinRecords

#### 签到记录 API 使用说明

**本地开发环境**:
```javascript
// 创建签到记录
POST /rating-api/api/classroom/checkin/:checkinId/record
Request Body:
{
  "uid": "用户UUID",
  "status": "present",  // present/absent/sick_leave/personal_leave
  "remark": "备注"      // 可选
}

// 更新签到记录
PUT /rating-api/api/classroom/checkin/record
Request Body:
{
  "recordId": 123,
  "status": "sick_leave",
  "remark": "备注"
}

// 获取签到记录列表
GET /rating-api/api/classroom/checkin/:checkinId/records
```

**线上部署环境**:
```javascript
// 创建签到记录
POST /api/classroom/checkin/:checkinId/record
Request Body:
{
  "uid": "用户UUID",
  "status": "present",
  "remark": "备注"
}

// 更新签到记录
PUT /api/classroom/checkin/record
Request Body:
{
  "recordId": 123,
  "status": "sick_leave",
  "remark": "备注"
}

// 获取签到记录列表
GET /api/classroom/checkin/:checkinId/records
```

**部署转换说明**:
- 前端代码中使用 `/rating-api/api/classroom/checkin/...`
- 部署时通过 Nginx 代理自动转换为 `/api/classroom/checkin/...`
- 无需修改前端代码,自动适配环境

#### 文档文件
- ✨ `本地和线上接口转换文档.md` - 本文档（统一版本）
- ✨ `restore-local.sh` - 本地开发恢复脚本
- ✨ `restore-production.sh` - 线上部署恢复脚本
- ✨ `DEPLOYMENT.md` - 完整部署文档
- ✨ `CONFIG_MIGRATION.md` - 配置迁移详细指南
- ✨ `CONFIG_DIFF.md` - 快速速查表
- ✨ `API_FIX_SUMMARY.md` - API 修复记录
- ✨ `FIX_SUMMARY.md` - 第一轮修复总结
- ✨ `README_CONFIG.md` - 文档导航

---

## 🔍 常见问题排查

### 问题 1: 404 Not Found (最常见)

**症状**: 访问 API 返回 404，响应头显示 `X-Powered-By: Express`

**原因**: Webpack 代理未生效

**解决方案**:

1. **检查代理配置顺序**:
   ```javascript
   // ✅ 正确顺序 (vue.config.js)
   proxy: {
     '/rating-api': { ... },  // 具体路由在前
     '/api': { ... }          // 通用路由在后
   }
   ```

2. **检查 pathRewrite 配置**:
   ```javascript
   // ✅ 正确配置
   pathRewrite: { '^/rating-api': '' }  // 空字符串

   // ❌ 错误配置
   pathRewrite: { '^/rating-api': '/api' }  // 会导致 /api/api/
   ```

3. **检查前端 API baseURL**:
   ```javascript
   // ✅ 正确配置
   baseURL: '/rating-api/api'  // 必须包含 /api

   // ❌ 错误配置
   baseURL: '/rating-api'  // 缺少 /api
   ```

4. **重启前端服务器**:
   ```bash
   # 停止当前服务
   ps aux | grep "npm run serve" | grep -v grep | awk '{print $2}' | xargs kill

   # 清除缓存
   cd hoj-vue && rm -rf node_modules/.cache

   # 重新启动
   npm run serve
   ```

### 问题 2: 数据库连接超时

**症状**: 间歇性 "invalid connection" 错误，每 2-3 小时出现一次

**原因**: MySQL 关闭空闲连接

**解决方案**: 已配置连接池（见 `hist-oj/configs/config.yaml`）

验证配置是否生效：
```bash
# 查看 hist-oj 日志
docker logs hist-oj | grep "数据库连接成功"

# 或查看本地运行日志
# 应该看到: "数据库连接成功 host=43.143.133.62 port=3306 database=hoj"
```

### 问题 3: 线上部署后前端未更新

**症状**: 部署后用户看到旧版本前端

**原因**: 浏览器缓存或 CDN 缓存

**解决方案**:
1. 强制刷新浏览器 (Ctrl+Shift+R / Cmd+Shift+R)
2. 清除 CDN 缓存（如果使用 CDN）
3. 验证 Nginx 缓存配置：
   ```bash
   curl -I http://your-domain.com/
   # 应看到: Cache-Control: no-cache, no-store
   ```

### 问题 4: 请求返回 Express 页面

**症状**: 响应头显示 `X-Powered-By: Express`，返回 HTML 而不是 JSON

**原因**: 代理未生效，请求被前端 dev server 处理

**解决方案**: 见"问题 1"的排查步骤

### 问题 5: CORS 错误

**症状**: 浏览器控制台显示 CORS 错误

**原因**: 跨域请求被阻止

**解决方案**: 本地开发不会出现 CORS（通过代理），线上部署已在 Nginx 配置 CORS 头

---

## ✅ 验证清单

### 本地开发验证

```bash
# 1. 检查 hist-oj 后端
curl http://localhost:9527/health
# 预期: {"status":"ok"}

# 2. 检查 Classroom API（需要登录）
curl http://localhost:8066/rating-api/api/classroom/user/roles
# 预期: {"code":401,"message":"用户未登录"}

# 3. 检查 Rating API（公开接口）
curl http://localhost:8066/rating-api/api/rating/user/1
# 预期: {"code":200,"data":{...}}

# 4. 检查 Battle API
curl http://localhost:8066/battle-api/api/battle/all-records?limit=20&currentPage=1&userId=1
# 预期: {"code":401,"message":"用户未登录"} 或返回数据

# 5. 检查 Judge API
curl -X POST http://localhost:8066/judge-api/api/judge/get-info \
  -H "Content-Type: application/json" \
  -d '{"pid":"1000"}'
# 预期: 返回题目信息或错误信息（不是404）

# 6. 检查 Rating Admin API
curl http://localhost:8066/rating-api/api/rating/admin/history?page=1&limit=20
# 预期: {"code":401,"message":"用户未登录"}
```

### 线上部署验证

```bash
# 1. 检查容器状态
docker-compose ps
# 预期: 所有服务都是 Up 状态

# 2. 检查 hist-oj 健康
docker exec hist-oj wget -q -O- http://localhost:9527/health
# 预期: {"status":"ok"}

# 3. 检查前端访问
curl -I http://your-domain.com/
# 预期包含: Cache-Control: no-cache

# 4. 检查 API 代理
curl http://your-domain.com/api/rating/user/1
# 预期: {"code":200,"data":{...}}

# 5. 检查所有容器日志
docker logs hist-oj --tail 50
docker logs hoj-frontend --tail 50
docker logs registration-backend --tail 50
```

### 验证成功标志

- ✅ 不应该返回 `404 page not found`
- ✅ 不应该返回 Express 页面 (`X-Powered-By: Express`)
- ✅ 未登录接口返回 `401` 是正常的（说明代理生效）
- ✅ 公开接口返回 `200` 和实际数据
- ✅ 直接访问后端 `http://localhost:9527/api/...` 应该有响应

---

## 🎯 核心要点总结

### 一句话总结

**本地开发和线上部署的区别只在于：**
1. `hist-oj/configs/config.yaml` 中的 `mode` 设置（`debug` vs `release`）
2. 前端运行方式（`npm run serve` vs `npm run build` + Docker）

**其他所有配置都已自动处理！**

### 关键配置规则

1. **代理 pathRewrite 必须是空字符串**
   ```javascript
   pathRewrite: { '^/rating-api': '' }  // ✅ 正确
   ```

2. **前端 API baseURL 必须包含 `/api`**
   ```javascript
   baseURL: '/rating-api/api'  // ✅ 正确
   baseURL: '/rating-api'      // ❌ 错误
   ```

3. **代理配置顺序很重要**
   ```javascript
   proxy: {
     '/rating-api': { ... },  // 具体路由
     '/api': { ... }          // 通用路由（放最后）
   }
   ```

4. **环境自动切换**
   ```javascript
   // ✅ 推荐：自动判断环境
   const baseURL = process.env.NODE_ENV === 'production'
     ? '/api'
     : '/rating-api/api'
   ```

---

## 📞 获取帮助

### 文档导航

- **快速上手**: 本文档的"快速切换"部分
- **详细说明**: 本文档的"详细配置说明"部分
- **API 规范**: 本文档的"API 路径规范"部分
- **问题排查**: 本文档的"常见问题排查"部分

### 常用命令

```bash
# 切换到本地开发
./restore-local.sh

# 切换到线上部署
./restore-production.sh

# 查看服务状态
docker-compose ps

# 查看日志
docker logs hist-oj -f
docker logs hoj-frontend -f

# 重启服务
docker-compose restart hist-oj
docker-compose restart hoj-frontend
```

### 日志位置

- **本地开发**: 终端输出
- **hist-oj**: `docker logs hist-oj` 或查看日志文件
- **前端**: `docker logs hoj-frontend`
- **浏览器**: F12 打开开发者工具，查看 Console 和 Network 面板

---

## 📝 版本历史

### v4.0 (2026-01-21)
- ✨ 新增签到记录管理功能
- ✨ 添加创建签到记录 API: `POST /checkin/:checkinId/record`
- ✨ 新增独立的签到记录页面组件
- ✨ 支持搜索、筛选、导出功能
- 📝 更新 API 转换文档,包含签到记录接口说明

### v3.0 (2026-01-20)
- 🐛 修复第三轮问题：Classroom API 路径错误
- ✨ 修复 `/api/classroom` → `/rating-api/api/classroom`
- ✨ 更新 vue.config.js 代理配置说明（确认端口 9527）
- ✨ 添加第三轮修复详细记录

### v2.0 (2026-01-20)
- ✨ 合并第一轮和第二轮修复内容
- ✨ 添加完整的 API 路径规范
- ✨ 添加详细的验证清单
- ✨ 添加常见问题排查指南

### v1.0 (2026-01-20)
- 🎉 初始版本
- ✨ 包含第一轮修复内容

---

**维护者**: Hist-OJ Team
**最后更新**: 2026-01-21
**下次更新**: 根据实际使用情况持续更新
