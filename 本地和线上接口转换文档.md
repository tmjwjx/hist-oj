# 接口路由现状

前端业务接口统一使用 `/api` 前缀，并由 `hoj-backend:6688` 提供。

已经并入 Java 主后端的扩展包括：

- 比赛报名与比赛内名称替换
- 题目数据同步与标准程序验题
- 比赛问答
- 训练报名与进度
- 代码对战
- 航海图
- 比赛代码查重

前端不得再使用 `/registration-api`、`/judge-api`、`/battle-api`、
`/training-api`、`/plagiarism-api` 或 `/contest-question-api`。

Rating、班级、作业、考试和资源功能也已统一由 Java 主后端提供。
开发和生产 Nginx 只保留通用 `/api` 代理，不再存在 Go 扩展服务或兼容代理。
