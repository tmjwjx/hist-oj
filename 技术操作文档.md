//清空所有rating和人工修改rating记录。
USE hoj;
TRUNCATE TABLE rating_history;
UPDATE user_record SET hist_rating = 1200 WHERE hist_rating IS NOT NULL;

UPDATE contest_rating_status
SET is_rated = 0,
    rating_calculated = 0,
    calculated_at = NULL
WHERE is_rated = 1 OR rating_calculated = 1;

UPDATE contest SET is_rating = 0 WHERE is_rating = 1;
TRUNCATE TABLE contest_skip_users;
TRUNCATE TABLE rating_recalculate_queue;
TRUNCATE TABLE rating_operation_logs;



//数据库表
submission_history ：用来维护判题终端
histcontest_register_competitions ：赛事系统比赛列表
histcontest_register_registrations ：赛事系统报名人员列表。
battle_room：对战房间表
battle_record：对战历史记录表
user_battle_stats：对战功能中每个用户的记录表
 
这个数据库表第一次部署需要赛事报名系统数据库的东西
/Users/zhuangqingjia/vscode/histoj/hist-oj/registration-system/migrations/001_add_last_view_time_fields.sql