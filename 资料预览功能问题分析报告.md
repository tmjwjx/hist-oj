# 资料预览功能问题分析报告

## 📋 问题描述

班级资料管理中，文件上传后无法在线预览，控制台报错：
- `[COS Viewer] 加载失败: undefined`
- `Uncaught ReferenceError: Hls is not defined`

## 🔍 问题分析过程

### 1. 前端检查

#### ❌ 问题1：缺少 hls.js 依赖

**位置**：`hoj-vue/src/components/COSDocViewer.vue:143-163`

**问题**：TCPlayer 需要 hls.js 支持，但代码中只加载了 TCPlayer，没有加载 hls.js 依赖

```javascript
loadTCPlayerScript() {
  // 只加载了 TCPlayer，缺少 hls.js ❌
  const script = document.createElement('script')
  script.src = 'https://web.sdk.qcloud.com/player/tcplayer/release/v4.5.0/tcplayer.v4.5.0.min.js'
  // ...
}
```

**影响**：视频文件无法播放

---

#### ❌ 问题2：前端拦截视频预览

**位置**：
- `hoj-vue/src/views/classroom/teacher/Materials.vue:1121-1124`
- `hoj-vue/src/views/classroom/student/Materials.vue:506-509`

**问题**：`canPreview` 函数不支持视频类型

```javascript
canPreview(fileType) {
  // 视频不支持在线预览 ⚠️
  const previewableTypes = ['pdf', 'txt', 'image', 'audio', 'ppt', 'word', 'excel']
  return previewableTypes.includes(fileType)  // ❌ 不包含 'video'
}
```

**影响**：教师端和学生端的视频文件"预览"按钮被隐藏

---

### 2. 后端检查

#### ❌ 问题3：后端API拦截视频预览

**位置**：`hist-oj/internal/api/classroom_api_part2.go:4922-4928`

**问题**：GetCOSPreviewUrl API 直接拒绝视频文件

```go
} else if fileType == "video" {
    // 视频类型：不支持在线预览 ⚠️
    logger.Info("视频文件不支持在线预览", ...)
    c.JSON(http.StatusOK, errorResponse(400, "视频文件不支持在线预览，请下载后观看"))
    return  // ❌ 直接返回错误
}
```

**影响**：即使前端绕过限制，后端API也会拒绝返回视频预览URL

---

#### ❌ 问题4：COS配置缺失（核心问题）

**服务器日志**：

```json
COS配置信息	{"bucket": "", "region": ""}
上传到COS失败	{"error": "上传到COS失败: invalid bucket format, please check your cos.BaseURL"}
```

**问题**：
1. 配置文件 `/opt/hist-oj/configs/config.yaml` 中没有 COS 配置
2. GetCOSPreviewUrl API 尝试上传文件到腾讯云COS时失败
3. API返回200状态码但实际返回错误（应该返回错误状态码）

**影响**：所有需要COS预览的文件（PDF/Office）都无法预览

---

## 📊 支持预览的文件类型清单

| 文件类型 | 格式 | 前端识别 | 后端识别 | 是否支持预览 | 预览方式 | 当前状态 |
|---------|------|---------|---------|------------|---------|---------|
| **视频** | mp4, webm, ogv, mov, avi, mkv, flv, m4v | ✅ | ✅ | ❌ 被拦截 | TCPlayer | ❌ 3层拦截 |
| 音频 | mp3, wav, aac, ogg, m4a, flac | ✅ | ✅ | ✅ | HTML5 Audio | ✅ 正常 |
| 图片 | jpg, jpeg, png, gif, bmp, svg, webp | ✅ | ✅ | ✅ | HTML5 Image | ✅ 正常 |
| PDF | pdf | ✅ | ✅ | ❌ COS缺失 | 腾讯云COS | ❌ 无配置 |
| Office | doc, docx, xls, xlsx, ppt, pptx | ✅ | ✅ | ❌ COS缺失 | 腾讯云COS | ❌ 无配置 |
| 文本 | txt, md | ✅ | ✅ | ❌ COS缺失 | 腾讯云COS | ❌ 无配置 |

---

## 🎯 拦截点汇总

### 拦截点1：前端UI层（教师+学生）
```javascript
canPreview(fileType) {
  const previewableTypes = ['pdf', 'txt', 'image', 'audio', 'ppt', 'word', 'excel']
  return previewableTypes.includes(fileType)  // ❌ 不包含 'video'
}
```

### 拦截点2：后端API层
```go
} else if fileType == "video" {
    c.JSON(http.StatusOK, errorResponse(400, "视频文件不支持在线预览"))
    return
}
```

### 拦截点3：COS配置缺失
```yaml
# config.yaml 中没有 COS 配置
cos:
  secret_id: ""   # ❌ 空值
  secret_key: ""  # ❌ 空值
  bucket: ""      # ❌ 空值
  region: ""      # ❌ 空值
```

---

## ✅ 解决方案

### 方案1：配置腾讯云COS（推荐）

**优点**：
- 支持PDF/Office文档在线预览
- 腾讯云COS提供文档预览服务

**步骤**：

1. 在服务器上编辑配置文件：
```bash
ssh root@43.143.133.62
vi /opt/hist-oj/configs/config.yaml
```

2. 添加COS配置：
```yaml
cos:
  secret_id: "你的腾讯云SecretId"
  secret_key: "你的腾讯云SecretKey"
  bucket: "你的存储桶名称"
  region: "ap-guangzhou"  # 根据实际区域调整
  base_url: "https://你的bucket名.cos.ap-guangzhou.myqcloud.com"
```

3. 重启hist-oj容器：
```bash
docker restart hist-oj
```

---

### 方案2：本地预览（无需COS）

**优点**：
- 无需腾讯云COS
- 视频音频图片可以直接预览

**缺点**：
- PDF/Office文档无法预览（需要COS文档预览服务）
- 仅支持视频、音频、图片本地预览

**需要修改的文件**：

#### 1. 移除前端视频拦截

**教师端**：`hoj-vue/src/views/classroom/teacher/Materials.vue:1121-1124`
```javascript
canPreview(fileType) {
  const previewableTypes = ['pdf', 'txt', 'image', 'audio', 'video', 'ppt', 'word', 'excel']
  return previewableTypes.includes(fileType)  // ✅ 添加 'video'
}
```

**学生端**：`hoj-vue/src/views/classroom/student/Materials.vue:506-509`
```javascript
canPreview(fileType) {
  const previewableTypes = ['pdf', 'txt', 'image', 'audio', 'video', 'ppt', 'word', 'excel']
  return previewableTypes.includes(fileType)  // ✅ 添加 'video'
}
```

#### 2. 移除后端API拦截

**文件**：`hist-oj/internal/api/classroom_api_part2.go:4922-4928`

```go
} else if fileType == "video" {
    // ✅ 移除拦截，返回视频URL直接预览
    previewURL = fileURL
    logger.Info("使用原始URL直接预览视频",
        zap.String("file_type", fileType),
        zap.String("direct_url", previewURL))
}
```

#### 3. 添加本地文件预览URL生成

**文件**：`hist-oj/internal/api/classroom_api_part2.go:4831-4949`

在 GetCOSPreviewUrl 函数中添加本地预览支持：

```go
// 如果没有配置COS，使用本地URL
if config.GlobalConfig.COS.Bucket == "" {
    // 生成临时访问token
    token := generatePreviewToken(materialID)

    // 构建本地预览URL
    scheme := "http"
    if c.Request.TLS != nil {
        scheme = "https"
    }
    host := c.Request.Host

    if fileType == "video" || fileType == "audio" || fileType == "image" {
        // 视频、音频、图片：直接返回本地URL
        previewURL = fmt.Sprintf("%s://%s/api/classroom/material/preview/%s", scheme, host, token)
    } else {
        // 文档类型：需要COS预览服务
        c.JSON(http.StatusOK, errorResponse(500, "未配置COS服务，无法预览文档"))
        return
    }
}
```

#### 4. 添加本地文件预览Handler

**文件**：`hist-oj/internal/api/routes.go`

```go
// 资料本地预览
classroom.GET("/material/preview/:token", AuthMiddleware(), handler.GetLocalMaterialPreview)
```

**文件**：`hist-oj/internal/api/classroom_api_part2.go`

```go
// GetLocalMaterialPreview 本地文件预览
func (h *Handler) GetLocalMaterialPreview(c *gin.Context) {
    token := c.Param("token")

    // 验证token并获取materialID
    materialID, err := verifyPreviewToken(token)
    if err != nil {
        c.JSON(http.StatusOK, errorResponse(401, "无效的预览token"))
        return
    }

    // 查询资料信息
    var material model.ClassroomMaterial
    db := client.GetDB()
    if err := db.Where("id = ? AND status = 1", materialID).First(&material).Error; err != nil {
        c.JSON(http.StatusOK, errorResponse(404, "文件不存在"))
        return
    }

    // 检查权限
    uid, exists := c.Get("userId")
    if !exists {
        c.JSON(http.StatusOK, errorResponse(401, "未登录"))
        return
    }

    canPreview, canDownload, _ := h.checkUserMaterialPermission(db, materialID, uid.(string))
    if !canPreview && !canDownload {
        c.JSON(http.StatusOK, errorResponse(403, "无预览权限"))
        return
    }

    // 返回本地文件
    localPath := "." + material.FilePath
    c.File(localPath)
}
```

---

## 📝 文件大小限制

| 用途 | 大小限制 | 位置 |
|-----|---------|------|
| 班级头像 | 10MB | `classroom_api_part2.go:2171` |
| 作业图片 | 10MB | `classroom_api_part2.go:3241` |
| 资料上传 | **200MB** | `scripts/nginx/bingoj.conf:49` |

---

## 🔧 已修复的问题

✅ **hls.js 依赖缺失** - 已在 `COSDocViewer.vue` 中添加

```javascript
// 先加载 hls.js（TCPlayer 依赖）
const hlsScript = document.createElement('script')
hlsScript.src = 'https://web.sdk.qcloud.com/player/tcplayer/release/v4.5.0/hls.min.js'
hlsScript.onload = () => {
    // hls.js 加载完成后，再加载 TCPlayer
    const tcplayerScript = document.createElement('script')
    tcplayerScript.src = 'https://web.sdk.qcloud.com/player/tcplayer/release/v4.5.0/tcplayer.v4.5.0.min.js'
    // ...
}
```

---

## 📌 需要修复的文件清单

如果选择**方案2（本地预览）**，需要修改以下文件：

1. ✅ `hoj-vue/src/components/COSDocViewer.vue` - 添加 hls.js 依赖（已完成）
2. ❌ `hoj-vue/src/views/classroom/teacher/Materials.vue` - 移除视频拦截
3. ❌ `hoj-vue/src/views/classroom/student/Materials.vue` - 移除视频拦截
4. ❌ `hist-oj/internal/api/classroom_api_part2.go` - 移除API拦截并添加本地预览
5. ❌ `hist-oj/internal/api/routes.go` - 添加本地预览路由

---

## 🚀 快速部署

如果选择方案2，可以执行以下命令快速部署：

```bash
cd /Users/zhuangqingjia/vscode/histoj/hist-oj

# 1. 前端构建
cd hoj-vue
npm run build:prod

# 2. 后端构建
cd ../hist-oj
make build

# 3. 服务器部署
ssh root@43.143.133.62 "docker stop hist-oj"
ssh root@43.143.133.62 "docker rm hist-oj"
scp hist-oj root@43.143.133.62:/opt/hist-oj/
ssh root@43.143.133.62 "cd /opt/hist-oj && docker-compose up -d hist-oj"
```

---

## 📞 联系方式

如有问题，请联系开发团队。

---

**生成时间**：2026-03-23
**服务器信息**：43.143.133.62
**数据库**：MySQL 8.0 (hoj)
**Docker容器**：hist-oj:latest
