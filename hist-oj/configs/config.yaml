server:
  port: 9527
  mode: release  # 生产环境使用 release

database:
  host: 43.143.133.62
  port: 3306
  user: root
  password: hist2025
  dbname: hoj

hoj_api:
  base_url: http://43.143.133.62:6688

jwt:
  # JWT 密钥，必须与 HOJ 后端 Nacos 配置中心中的 hoj.jwt.secret 保持一致
  # 来源：sqlAndsetting/nacos.sql 中的初始化配置，默认值为 hoj-secret-init
  secret: hoj-secret-init

rating:
  initial_rating: 0  # 新用户初始 Rating
  k_factor: 85         # K 值，控制波动幅度（已调大）
  check_interval: 5     # 定时检查间隔（分钟）

# 腾讯云COS配置（用于文档预览）
cos:
  secret_id: "AKIDnDkARguzugIhw1xqaJ1gDoQqAiBVucFo"
  secret_key: "gDj3A6P1UnxxkLl04hFsLbeXGoFUVN4m"
  bucket: "histoj-ppt-preview-1380268413"
  region: "ap-nanjing"
  # CDN加速域名（开启CDN后自动分配，可节省约70%流量费用）
  # 开通CDN后，自动分配域名如：histoj-ppt-preview-1380268413.cdncdn.me
  # 或：histoj-ppt-preview-1380268413.file.myqcloud.com
  cdn_domain: ""  # 开通CDN后填写自动分配的域名，留空则不使用CDN
