# 训练功能线上部署转换清单

> **生成时间**: 2026-01-21
> **版本**: v1.0
> **适用环境**: 从本地开发环境切换到线上生产环境

---

## ✅ 检查结果总结

### 训练功能相关文件

所有训练功能的API配置都已经正确设置,无需修改!

| 文件 | 状态 | 说明 |
|------|------|------|
| `/src/api/training.js` | ✅ 已正确 | 使用 `/rating-api/api` |
| `/src/views/oj/training/TrainingDetails.vue` | ✅ 已正确 | 通过API模块调用 |
| `/src/views/admin/training/TrainingParticipants.vue` | ✅ 已正确 | 通过API模块调用 |
| `/src/views/oj/training/TrainingList.vue` | ✅ 已正确 | 通过API模块调用 |
| `/src/store/training.js` | ✅ 已正确 | 通过API模块调用 |

---

## 🔧 转换原理

### 本地开发环境

```
前端请求: /rating-api/api/training/{id}/join
    ↓ (Webpack DevServer Proxy)
代理到: http://localhost:9527/api/training/{id}/join
    ↓
hist-oj后端处理 (运行在Docker容器端口9527)
```

### 线上部署环境

```
前端请求: /rating-api/api/training/{id}/join
    ↓ (Nginx Proxy)
转发到: http://hist-oj:9527/api/training/{id}/join
    ↓
hist-oj后端处理 (运行在Docker容器)
```

**重要**: 前端代码完全不需要修改! Nginx会自动处理路径转换。

---

## 📋 部署前检查清单

### 1. 后端配置检查

**文件**: `hist-oj/configs/config.yaml`

```yaml
# ✅ 确认以下配置
server:
  port: 9527
  mode: release  # 线上必须是 release

database:
  host: 43.143.133.62
  port: 3306
  user: root
  password: hist2025
  dbname: hoj
  max_open_conns: 100
  max_idle_conns: 10
  conn_max_lifetime: 1800
  conn_max_idletime: 600

hoj_api:
  base_url: http://43.143.133.62:6688
```

### 2. 训练API接口确认

**文件**: `hoj-vue/src/api/training.js`

```javascript
// ✅ 正确配置 - 无需修改
const BASE_URL = '/rating-api/api'

export function joinTraining(trainingId) {
  return axios.post(`${BASE_URL}/training/${trainingId}/join`)
}

export function getTrainingParticipants(trainingId) {
  return axios.get(`${BASE_URL}/training/${trainingId}/participants`)
}

export function getMyTrainingRecord(trainingId) {
  return axios.get(`${BASE_URL}/training/${trainingId}/my-record`)
}
```

### 3. Nginx配置检查

**文件**: `hoj-vue/nginx.conf`

```nginx
# ✅ 确认以下配置存在
location /rating-api/ {
    proxy_pass http://hist-oj:9527/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
```

### 4. 后端路由确认

**文件**: `hist-oj/internal/api/routes.go`

确保以下路由已注册:

```go
// ✅ 训练相关路由
trainingRouter.POST("/:id/join", api.JoinTraining)           // 参加训练
trainingRouter.GET("/:id/participants", api.GetTrainingParticipants)  // 获取参与者列表
trainingRouter.GET("/:id/my-record", api.GetMyTrainingRecord)  // 获取我的训练记录
```

---

## 🚀 部署步骤

### 方式一: 使用自动化脚本(推荐)

```bash
# 1. 切换到线上配置
./restore-production.sh

# 2. 构建前端
cd hoj-vue
npm run build

# 3. 部署到服务器
cd ..
./scripts/quick-deploy.sh all
```

### 方式二: 手动部署

```bash
# 1. 修改后端配置
vim hist-oj/configs/config.yaml
# 修改: mode: release

# 2. 构建前端
cd hoj-vue
npm run build

# 3. 停止现有服务
docker-compose down

# 4. 重新构建并启动
docker-compose up -d --build

# 5. 查看日志确认启动成功
docker logs hist-oj -f
docker logs hoj-frontend -f
```

---

## 🧪 部署后验证

### 1. 检查容器状态

```bash
docker-compose ps
# 预期: 所有服务都是 Up 状态
```

### 2. 测试训练API接口

```bash
# 公开接口测试
curl https://你的域名/api/training/1/participants

# 应该返回:
# {"code":200,"message":"success","data":[...]}

# 参加训练接口(需要登录token)
curl -X POST https://你的域名/api/training/1/join \
  -H "Authorization: Bearer YOUR_TOKEN"
```

### 3. 功能测试

在浏览器中测试以下功能:

- [ ] 训练列表页面能正常加载
- [ ] 点击训练详情能正常打开
- [ ] "参加训练"按钮能正常工作
- [ ] 管理员能查看参与者列表
- [ ] 参与者状态显示正常
- [ ] 完成进度显示正常

---

## 📊 API路径对照表

| 功能 | 本地开发路径 | 线上部署路径 | 后端服务 |
|------|-------------|-------------|----------|
| **参加训练** | `/rating-api/api/training/:id/join` | `/api/training/:id/join` | hist-oj:9527 |
| **获取参与者列表** | `/rating-api/api/training/:id/participants` | `/api/training/:id/participants` | hist-oj:9527 |
| **获取我的训练记录** | `/rating-api/api/training/:id/my-record` | `/api/training/:id/my-record` | hist-oj:9527 |

---

## 🐛 常见问题排查

### 问题1: 参加训练按钮点击无响应

**症状**: 点击"参加训练"按钮后没有任何反应

**排查步骤**:
1. 打开浏览器F12开发者工具
2. 查看Console是否有错误信息
3. 查看Network面板,找到 `join` 请求
4. 检查请求URL是否正确 (应该是 `/rating-api/api/training/:id/join`)
5. 检查响应状态码

**解决方案**:
- 404: 检查Nginx配置是否正确
- 401: 检查用户是否已登录
- 500: 查看后端日志 `docker logs hist-oj -f`

### 问题2: 参与者列表显示为空

**症状**: 训练详情页或参与者页面显示"暂无参与者"

**可能原因**:
1. 该训练确实没有参与者
2. API请求失败
3. 权限不足

**排查步骤**:
```bash
# 直接调用API测试
curl https://你的域名/api/training/1/participants

# 检查返回数据
```

### 问题3: 线上部署后前端显示旧版本

**解决方案**:
1. 强制刷新浏览器 (Ctrl+Shift+R / Cmd+Shift+R)
2. 清除浏览器缓存
3. 重新构建前端: `npm run build`
4. 重启前端容器: `docker-compose restart hoj-frontend`

---

## 📝 相关文档

- **API转换文档**: `本地和线上接口转换文档.md`
- **完整部署文档**: `DEPLOYMENT.md`
- **配置迁移指南**: `CONFIG_MIGRATION.md`
- **快速参考表**: `CONFIG_DIFF.md`

---

## ✨ 总结

### 核心要点

1. **代码无需修改**: 所有训练功能的API配置都已经是正确的
2. **自动转换**: Nginx会自动将 `/rating-api/api` 转换为 `/api`
3. **配置切换**: 只需要修改 `hist-oj/configs/config.yaml` 中的 `mode`
4. **重新构建**: 使用 `npm run build` 构建生产版本

### 部署检查项

- [ ] 后端配置 mode: release
- [ ] 前端已执行 npm run build
- [ ] Nginx配置正确
- [ ] Docker容器都正常运行
- [ ] API接口测试通过
- [ ] 功能测试通过

---

**维护者**: Hist-OJ Team
**最后更新**: 2026-01-21
**状态**: ✅ 训练功能已准备好线上部署
